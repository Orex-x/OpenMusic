<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.OpenMusic" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:textColor">@color/white</item>
        <item name="background_color_light">@color/back_light_night</item>
        <item name="background_color">@color/back_night</item>
    </style>
</resources>